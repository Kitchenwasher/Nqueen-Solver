import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "About",
  description: "About QueenMind, the creator profile, product vision, and architecture philosophy.",
  alternates: {
    canonical: "/about"
  }
};

export default function AboutPage() {
  return null;
}
