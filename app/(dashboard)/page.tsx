import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Solver Workspace",
  description: "Interactive N-Queen solver workspace with constraints, telemetry, and visual exploration.",
  alternates: {
    canonical: "/"
  }
};

export default function HomePage() {
  return null;
}
