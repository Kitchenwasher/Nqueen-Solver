import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Benchmark Lab",
  description: "Run comparative benchmark sweeps and stress diagnostics across N-Queen solver strategies.",
  alternates: {
    canonical: "/benchmark"
  }
};

export default function BenchmarkPage() {
  return null;
}
