import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Insights Lab",
  description: "Explore telemetry trends, algorithm performance, and strategy behavior across solver runs.",
  alternates: {
    canonical: "/insights"
  }
};

export default function InsightsPage() {
  return null;
}
